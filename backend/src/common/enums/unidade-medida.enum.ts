export enum UnidadeMedida {
    // Peso
    KG = 'kg',
    G = 'g',
    MG = 'mg',

    // Volume
    L = 'l',
    ML = 'ml',

    // Unidade
    UN = 'un',
    PCT = 'pct',
    CX = 'cx',

    // Outros
    DENTE = 'dente', // Alho
    FOLHA = 'folha', // Manjericão
    RAMO = 'ramo',
}